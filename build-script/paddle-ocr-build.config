# build.config
# 도커 레지스트리
DOCKER_REGISTRY=mncregistry:30500

# 이미지 이름
IMAGE_NAME=doc-parser-ocr

# 버전 (PaddleX Version)
IMAGE_VERSION=3.3.6

# 실제 Dockerfile 위치 (루트 기준)
DOCKERFILE_PATH=genon/serving/paddle/docker/Dockerfile

# 빌드 후 push 할지 여부
PUSH_IMAGE=true