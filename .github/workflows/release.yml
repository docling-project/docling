name: Release checks (release/*)
on:
  push:
    branches: ['release/**']
  workflow_dispatch: {}

jobs:
  smoke:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with: { python-version: '3.12' }
      - uses: astral-sh/setup-uv@v6
      - run: uv python pin 3.12
      - name: Sync deps
        working-directory: genon/preprocessor
        run: uv sync --dev
      - name: Download custom layout model from HuggingFace
        working-directory: genon/preprocessor
        run: |
          uv run python -c "
          from docling.models.layout_model import LayoutModel
          from docling.datamodel.layout_model_specs import MNCAI_CUSTOM_LAYOUT
          print('Downloading custom layout model...')
          model_path = LayoutModel.download_models(layout_model_config=MNCAI_CUSTOM_LAYOUT, progress=True)
          print(f'Model downloaded to: {model_path}')
          "
      - name: Import smoke
        working-directory: genon/preprocessor
        run: uv run python -c "import importlib; importlib.import_module('facade.basic_processor')"
      - name: Pytest (smoke only)
        working-directory: genon/preprocessor
        run: uv run pytest -m "smoke" -v -ra --durations=5
      - name: Build package (optional)
        working-directory: genon/preprocessor
        run: uv build
